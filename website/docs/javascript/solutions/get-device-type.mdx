---
title: 获取设备类型
---

## 使用 userAgent 判断 Mobile 设备

### 识别手机端

```javascript
// Detect device type
const isMobileDevice = /Mobi|Android|iPhone/i.test(navigator.userAgent);
```

⚠️ 该方法识别出的移动端设备不包括 **iPad**。

### 识别 iPad

```javascript
const isiPad = /iPad/i.test(navigator.userAgent);
```
