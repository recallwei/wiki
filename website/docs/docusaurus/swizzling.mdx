---
title: Swizzling
---

Docusaurus 允许你使用 **Swizzling** 对网站进行深度自定义。

## Ejecting

**Ejecting** 会创建一个可以完全自定义的组件副本。

## Wrapping

**Wrapping** 会创建一个原组件的包装器，可以增强该组件的包装。

## 查看可以 Swizzle 的组件列表

```bash
yarn run swizzle -- --list
```
