---
title: String
---

import TH from "@site/src/components/TextHighlight/index";

String 需要在双引号中定义值，而单引号用于定义单个字符以及 runes。

## 转义字符

对字符进行转义，在其前面添加<TH>反斜杠</TH>（\）。

- `\n` - 换行符
- `\r` - 回车符
- `\t` - 制表符
- `\'` - 单引号
- `\"` - 双引号
- `\\` - 反斜杠
