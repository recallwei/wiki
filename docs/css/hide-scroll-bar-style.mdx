---
title: 隐藏滚动条样式
---

为可滚动元素添加如下样式，即 `class="scroll"`，可隐藏滚动条样式。

```css title="CSS"
.scroll::-webkit-scrollbar {
  display: none; /* Chrome Safari */
}
.scroll {
  scrollbar-width: none; /* firefox */
  -ms-overflow-style: none; /* IE 10+ */
  overflow-x: hidden;
  overflow-y: auto;
}
```
