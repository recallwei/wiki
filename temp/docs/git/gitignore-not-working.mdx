---
title: ".gitignore 不工作"
---

## 原因

我想要将 `.env.development` 文件添加到 `.gitignore`，但是 GitHub main 分支上追踪了该文件，导致本地将该文件添加进 `.gitignore` 不生效，还是可以将 `.env.development` 文件 commit 到远程。

## 解决方案

执行 `git rm --cached [filename]`，该指令将会删除当前文件的 git 历史记录。

再次将该文件添加到 `.gitignore`，接着提交更改，该文件成功被 Git 忽略。
